function [ vetor ] = check( imagem, scribble )
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here

img = imread(imagem);
scb = imread(scribble);
[N M] = size(img);
vetor = 0;
for i=1:N
    for j=1:N
        if img(i,j) ~= srb(i,j)
          vetor(i)   
    end
end

end

